# Selected/ — Deprecated

The individual `Deck_Dark_Page_*.html` template files in this folder are no longer used by the skill.

**Replaced by:**
- `design-system.css` — contains all 8 template classes (`tmpl-cover`, `tmpl-center`, `tmpl-twocol`, `tmpl-compare`, `tmpl-features`, `tmpl-content-img`, `tmpl-stats`, `tmpl-steps`) as reusable CSS
- `slide-templates.html` — standalone visual reference showing all 8 templates with live scaled-down previews

**Why:** Strategy A (reading individual template files) was removed from the skill. All presentations are now generated using Strategy B (embedded template classes in design-system.css), which eliminates the need to read any file in this folder.

These files are kept for historical reference only.
